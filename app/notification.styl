@-webkit-keyframes flyOut { from { right:-100%; } to { right:0%; } }
// @-moz-keyframes flyOut { from { opacity:0; } to { opacity:1; } }
// @keyframes flyOut { from { opacity:0; } to { opacity:1; } }

#notification
  padding: 0
  margin: 0
  list-style: none
  position: absolute
  top:0
  right:0
  li
    position:relative
    color: #fff
    background: rgba(34,34,34,.7)
    border-radius: 15px
    margin: 20px
    padding: 10px
    transition: padding 0.5s
    h4
      margin: 0
      color: #eee
      font-size: 14px
      font-weight: bold
      padding: 0 0 7px 0
      display: block
      text-shadow: 1px 1px 0 #000
    &:after
      position:absolute
      top: 3px
      left: 3px
      border-radius: 50%
      border:2px solid #fff
      color: #fff
      width: 20px
      height: 20px
      display: none
      text-align: center
      line-height: 20px
      font-size: 16px
      background: #000
      box-shadow: 1px 1px #000
      content: '×' //TODO add from font
    &:hover:after
      display:block